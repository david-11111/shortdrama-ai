# AI 判断偏差防控规范

> 本规范源自实际项目教训。AI 在独立判断时存在系统性偏差，
> 需通过规则强制约束，避免"看起来有进展、实际上没进展"的虚假闭环。

---

## 一级偏差：修复惯性

**现象：** 遇到组件故障时，AI 默认选择"修好它"，而不是评估"值不值得修、有没有更快的路"。

**案例：** Infer API 404，AI 花数小时排查 upload_to_comfyui 的 bug，而绕过它只需改一行代码。

**规则：**
```
当 A 组件出现阻塞性故障时，必须先回答：
1. A 是否是唯一路径？有无 B、C 路径可以绕过？
2. 绕过成本（代码行数） vs 修复成本（排查时间）的对比
3. 如果绕过成本 < 修复成本 × 0.3，强制选绕过方案
```

---

## 二级偏差：过程替代结果

**现象：** AI 把"完成了某个步骤"当成"完成了目标"，忽略步骤到目标之间的验证缺口。

**案例：** Infer API 返回了 job_id → AI 认为"链路通了"，但实际上 poll 一直是 404，视频从未生成过。

**规则：**
```
每个步骤必须定义"真实验证物"，不能以中间产物替代：
- ❌ "API 返回了 job_id" = 通了
- ✅ "API 返回了视频下载 URL 且能下载" = 通了
- ❌ "ComfyUI 之前跑成功过" = 现在也能跑
- ✅ "当前 session 我们亲自跑成功一次" = 能跑
```

**验证闭环清单（必须逐项标记）：**
| 步骤 | 伪验证 | 真实验证 |
|------|--------|----------|
| API 调通 | 返回 200 | 返回业务预期数据 |
| 视频生成 | job_id 返回 | 视频文件落地可下载 |
| 链路通 | 某组件日志显示成功 | 本次请求的完整链路追踪 |

---

## 三级偏差：确定性伪装

**现象：** AI 在不确定时倾向于给出自信的猜测而不是承认不确定。

**案例：** "可能是图片 URL 过期"——实际上 AI 并不知道根因，只是猜的。

**规则：**
```
当 AI 对根因不确定时，必须使用以下格式：
1. "我不知道根因，需要做 X 来确认"
2. 禁止用"可能是...""也许是..."作为结论
3. 不确定时，必须提供确认方法（加日志、加测试、换路径）
```

---

## 四级偏差：优先级倒挂

**现象：** AI 在核心链路未通的情况下，优先做非核心的优化和加固。

**案例：** 视频生成还没跑通，先修了 orphan reconciler 和状态机门控。

**规则：**
```
在任何时刻，优先级必须遵循：
P0: 核心链路跑通（视频能生成 → 能剪辑 → 能出片）
P1: 核心链路稳定（异常处理、重试、降级）
P2: 核心链路可观测（日志、指标、告警）
P3: 非核心优化

核心链路未通时，禁止处理 P1-P3 事项。
```

---

## 五级偏差：本地假设远程

**现象：** AI 在本地环境做的测试结论，默认假设远程环境也一样。

**案例：** 本地容器 curl wan-tunnel:8100 返回 job_id → 默认假设服务器端也会正常执行。

**规则：**
```
远程执行与本地执行之间必须做一次"真实穿透验证"：
1. 本地能调通 → 远程对应服务是否在线
2. 远程 API 返回成功 → 远程实际工作流是否执行成功
3. 远程执行成功 → 结果是否能回传本地

每层穿透必须分别验证，禁止跳跃推理。
```

---

## 应用场景

本规范应在以下环节自动载入：
1. AI 大脑做技术决策时（作为 system prompt 注入）
2. 状态机做阶段推进判断时（作为 gate 条件引入）
3. 每次 AI 给出"进展汇报"时（自动检测是否违反上述规则）

## 违规响应

当任何环节检测到上述偏差时，系统必须：
1. 立即中断当前判断路径
2. 输出违反的偏差编号和具体表现
3. 强制要求人类确认后才能继续
